# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RouteHealthStatus(BaseModel):
    """
    Per-mapping annotation: the route is currently suspended by passive-health cooldown. Serialised as `route_health_status` on each listing row (omitted entirely when the route is healthy).
    """ # noqa: E501
    message: StrictStr = Field(description="Human-readable summary suitable for a tooltip.")
    reason: StrictStr = Field(description="Coarse cause: `\"rate_limited\"` | `\"provider_errors\"` | `\"auth_failed\"` | `\"unavailable\"` (the last when only a cross-pod deadline is known). `\"auth_failed\"` only ever pairs with `scope = \"current_credential\"` — see [`status_for_cooldown`].")
    recovers_in_secs: Annotated[int, Field(strict=True, ge=0)] = Field(description="Approx seconds until the model is usable again.")
    scope: StrictStr = Field(description="Who the suspension affects: `\"all_users\"` for a shared route cooldown or `\"current_credential\"` for the authenticated caller's passthrough credential.")
    state: StrictStr = Field(description="`\"suspended\"` (in cooldown) or `\"recovering\"` (probing recovery).")
    __properties: ClassVar[List[str]] = ["message", "reason", "recovers_in_secs", "scope", "state"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RouteHealthStatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RouteHealthStatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "message": obj.get("message"),
            "reason": obj.get("reason"),
            "recovers_in_secs": obj.get("recovers_in_secs"),
            "scope": obj.get("scope"),
            "state": obj.get("state")
        })
        return _obj


