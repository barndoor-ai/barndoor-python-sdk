# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from uuid import UUID
from barndoor.models.long_context_tier import LongContextTier
from barndoor.models.pricing_change_source import PricingChangeSource
from barndoor.models.pricing_sync_mode import PricingSyncMode
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PricingRuleSummary(BaseModel):
    """
    One entry in the per-rule list returned by `GET /admin/model-pricing`. `current` is the latest version with `effective_from <= now()` (so the row UI can render exactly as it did pre-V34), `scheduled_count` / `next_scheduled_effective_from` drive the small \"Scheduled\" affordance on the row, and `version_count` decides whether to show the history icon.
    """ # noqa: E501
    cache_read_cost_per_million_tokens: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Per-million-token cost for input tokens served from the prompt cache. `None` ⇒ the calculator bills cached input at `input_cost_per_million_tokens`.")
    cache_write_cost_per_million_tokens: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Per-million-token cost for input tokens added to the prompt cache. `None` ⇒ same fallback as `cache_read_cost_per_million_tokens`.")
    catalog_slug: Optional[StrictStr] = Field(default=None, description="Catalog slug for provider-level pricing (e.g. \"openai\", \"aws_bedrock\").")
    effective_from: datetime
    id: UUID
    input_cost_per_million_tokens: Union[StrictFloat, StrictInt]
    long_context: Optional[LongContextTier] = Field(default=None, description="Optional context-tiered surcharge (BCP-3904). `None` ⇒ one rate set at every prompt size, i.e. exactly the pre-BCP-3904 behaviour.  Serialized as `long_context`, not as the Rust field name. This struct reaches the admin API through `AdminModelPricing`'s `#[serde(flatten)]`, so the field name here *is* the wire key — and every sibling surface (`DefaultPricingEntry`, `EffectivePricing`, the create/update request bodies) already spells it `long_context`. Left unrenamed, the pricing tab would write a tier successfully and then read back nothing, which is precisely how it behaved before this attribute existed: the surcharge saved, the dialog reopened unchecked, and the edit looked silently discarded. Pinned by `admin_model_pricing_serializes_the_tier_as_long_context`.")
    model_pattern: StrictStr
    model_provider: Optional[StrictStr] = None
    org_id: UUID
    output_cost_per_million_tokens: Union[StrictFloat, StrictInt]
    provider_id: Optional[UUID] = Field(default=None, description="Optional reference to a specific LLM Gateway provider instance.")
    provider_name: Optional[StrictStr] = None
    change_reason: Optional[StrictStr] = None
    change_source: PricingChangeSource
    created_by_email: Optional[StrictStr] = None
    created_by_name: Optional[StrictStr] = None
    created_by_user_id: Optional[UUID] = None
    is_archived: StrictBool = Field(description="True for tombstone rows produced by `archive_rule` (V35). When the latest effective version of a group has `is_archived = true`, the rule is considered archived: the cost calculator skips it and the admin summary list hides it by default. Past non-archived versions remain in the timeline so the History dialog can show the rule's full life including the archive event.")
    sync_mode: PricingSyncMode
    next_scheduled_effective_from: Optional[datetime] = Field(default=None, description="When the next scheduled version takes effect, if any.")
    scheduled_count: StrictInt = Field(description="Versions of this rule with `effective_from > now()` that haven't activated yet. `0` means no scheduled change pending.")
    version_count: StrictInt = Field(description="Total versions in this rule's history (including `current`).")
    __properties: ClassVar[List[str]] = ["cache_read_cost_per_million_tokens", "cache_write_cost_per_million_tokens", "catalog_slug", "effective_from", "id", "input_cost_per_million_tokens", "long_context", "model_pattern", "model_provider", "org_id", "output_cost_per_million_tokens", "provider_id", "provider_name", "change_reason", "change_source", "created_by_email", "created_by_name", "created_by_user_id", "is_archived", "sync_mode", "next_scheduled_effective_from", "scheduled_count", "version_count"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PricingRuleSummary from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of long_context
        if self.long_context:
            _dict['long_context'] = self.long_context.to_dict()
        # set to None if next_scheduled_effective_from (nullable) is None
        # and model_fields_set contains the field
        if self.next_scheduled_effective_from is None and "next_scheduled_effective_from" in self.model_fields_set:
            _dict['next_scheduled_effective_from'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PricingRuleSummary from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cache_read_cost_per_million_tokens": obj.get("cache_read_cost_per_million_tokens"),
            "cache_write_cost_per_million_tokens": obj.get("cache_write_cost_per_million_tokens"),
            "catalog_slug": obj.get("catalog_slug"),
            "effective_from": obj.get("effective_from"),
            "id": obj.get("id"),
            "input_cost_per_million_tokens": obj.get("input_cost_per_million_tokens"),
            "long_context": LongContextTier.from_dict(obj["long_context"]) if obj.get("long_context") is not None else None,
            "model_pattern": obj.get("model_pattern"),
            "model_provider": obj.get("model_provider"),
            "org_id": obj.get("org_id"),
            "output_cost_per_million_tokens": obj.get("output_cost_per_million_tokens"),
            "provider_id": obj.get("provider_id"),
            "provider_name": obj.get("provider_name"),
            "change_reason": obj.get("change_reason"),
            "change_source": obj.get("change_source"),
            "created_by_email": obj.get("created_by_email"),
            "created_by_name": obj.get("created_by_name"),
            "created_by_user_id": obj.get("created_by_user_id"),
            "is_archived": obj.get("is_archived"),
            "sync_mode": obj.get("sync_mode"),
            "next_scheduled_effective_from": obj.get("next_scheduled_effective_from"),
            "scheduled_count": obj.get("scheduled_count"),
            "version_count": obj.get("version_count")
        })
        return _obj
