# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
from enum import Enum
from typing_extensions import Self


class ScopeType(str, Enum):
    """
    Scope is the *identity* axis of a budget or rate-limit policy — \"who is this policy for?\". The *what* (LLM provider, upstream model name, model alias) lives on dedicated `target_*` columns on the row (BCP-2813), not as a scope value. `LlmProvider` and `Model` remain in the enum because `rate_limit_policies` and `model_access_policies` still accept them in their `scope_type` CHECK constraints (V12 / V18 / V22 widenings — a separate scope of work to clean up). For token budgets the CHECK was tightened back to identity-only by `V41__budget_scope_target.sql`, so any `LlmProvider`/`Model` budget row that survived would simply fail to match in `BudgetEnforcer::matched_keys`.
    """

    """
    allowed enum values
    """
    ORG = 'org'
    TEAM = 'team'
    USER = 'user'
    PROJECT = 'project'
    API_KEY = 'api_key'
    MCP_SERVER = 'mcp_server'
    LLM_PROVIDER = 'llm_provider'
    MODEL = 'model'
    AGENT = 'agent'
    ROLE = 'role'
    GROUP = 'group'

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of ScopeType from a JSON string"""
        return cls(json.loads(json_str))


