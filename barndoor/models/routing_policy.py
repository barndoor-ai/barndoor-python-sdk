# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from barndoor.models.posture import Posture
from barndoor.models.routing_slot import RoutingSlot
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RoutingPolicy(BaseModel):
    """
    RoutingPolicy
    """ # noqa: E501
    context_breakpoints: List[StrictInt]
    created_at: datetime
    default_slot_on_failure: StrictInt
    description: Optional[StrictStr] = None
    determiner_model_alias: Optional[StrictStr] = Field(default=None, description="The LLM that picks a slot. Optional because a policy may be saved before one is chosen; the picker falls back to the context-size floor when it is absent.")
    determiner_prompt: Optional[StrictStr] = Field(default=None, description="Optional override for the determiner's instruction preamble. `None` (or empty) falls back to the built-in default (see `routing::determiner`). The slot catalog, request-metadata block, and JSON-output schema are always appended by the gateway so the structured-output contract holds regardless of what an admin writes here.")
    enabled: StrictBool
    id: UUID
    model_alias: StrictStr
    org_id: UUID
    posture: Posture = Field(description="How eagerly this policy steps up when the router is unsure.")
    router_input_max_chars: StrictInt
    slots: List[RoutingSlot]
    updated_at: datetime
    __properties: ClassVar[List[str]] = ["context_breakpoints", "created_at", "default_slot_on_failure", "description", "determiner_model_alias", "determiner_prompt", "enabled", "id", "model_alias", "org_id", "posture", "router_input_max_chars", "slots", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RoutingPolicy from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in slots (list)
        _items = []
        if self.slots:
            for _item_slots in self.slots:
                _items.append(_item_slots.to_dict() if _item_slots is not None else None)
            _dict['slots'] = _items
        # set to None if description (nullable) is None
        # and model_fields_set contains the field
        if self.description is None and "description" in self.model_fields_set:
            _dict['description'] = None

        # set to None if determiner_model_alias (nullable) is None
        # and model_fields_set contains the field
        if self.determiner_model_alias is None and "determiner_model_alias" in self.model_fields_set:
            _dict['determiner_model_alias'] = None

        # set to None if determiner_prompt (nullable) is None
        # and model_fields_set contains the field
        if self.determiner_prompt is None and "determiner_prompt" in self.model_fields_set:
            _dict['determiner_prompt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RoutingPolicy from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "context_breakpoints": obj.get("context_breakpoints"),
            "created_at": obj.get("created_at"),
            "default_slot_on_failure": obj.get("default_slot_on_failure"),
            "description": obj.get("description"),
            "determiner_model_alias": obj.get("determiner_model_alias"),
            "determiner_prompt": obj.get("determiner_prompt"),
            "enabled": obj.get("enabled"),
            "id": obj.get("id"),
            "model_alias": obj.get("model_alias"),
            "org_id": obj.get("org_id"),
            "posture": obj.get("posture"),
            "router_input_max_chars": obj.get("router_input_max_chars"),
            "slots": [RoutingSlot.from_dict(_item) for _item in obj["slots"]] if obj.get("slots") is not None else None,
            "updated_at": obj.get("updated_at")
        })
        return _obj
