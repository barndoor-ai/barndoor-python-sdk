# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from barndoor.models.channel_subscription_input import ChannelSubscriptionInput
from barndoor.models.channel_type import ChannelType
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ChannelUpsertRequest(BaseModel):
    """
    Create or update one notification channel and replace its subscription set.  This is a full upsert, not a patch: ``subscriptions`` is **replaced** by what you send, so omitting it removes every subscription the channel had. Send the complete desired set on every call — which is what makes the endpoint safe to drive from a declarative tool.  Which fields are permitted depends on ``type``; sending one that does not belong to the type is a ``422``, not a silently ignored field. See ``ChannelType`` for the full matrix.  | ``type`` | required | forbidden | |---|---|---| | ``in_app`` / ``user_email`` | — | every destination field | | ``email`` | ``email_address`` | ``url``, ``label``, ``slack_channel_id``, ``teams_workflow_url`` | | ``webhook`` | ``url`` | ``email_address``, ``label``, ``slack_channel_id``, ``teams_workflow_url`` | | ``slack`` | ``slack_channel_id``, ``label`` | ``email_address``, ``url``, ``teams_workflow_url`` | | ``teams`` | ``label`` (+ ``teams_workflow_url`` on create) | ``email_address``, ``url``, ``slack_channel_id`` |  **Identity.** Without ``id`` the channel is keyed on its type's natural identity, so repeating the same call is idempotent rather than creating duplicates: personal types by (organization, type, caller), ``email`` by (organization, ``email_address``), ``webhook`` by (organization, ``url``), ``slack`` by (organization, ``slack_channel_id``). With ``id`` it is an authoritative edit of that row — the only way to change a ``teams`` channel, whose natural identity is a secret URL you never hold back.
    """ # noqa: E501
    id: Optional[UUID] = Field(default=None, description="Existing channel to edit authoritatively. Omit to create-or-dedup on the type's natural identity (see the model description). When supplied and unknown — or owned by another organization — the request is a `404`, never a create.")
    type: ChannelType = Field(description="The kind of destination, and the discriminator for every other field. Immutable in practice: an edit-by-id that changes `type` is rejected, and an upsert without `id` and a different `type` addresses a different natural identity, so it creates a separate channel.")
    enabled: Optional[StrictBool] = Field(default=True, description="Whether the channel delivers. `false` suspends delivery while keeping the channel and its subscriptions intact — the reversible alternative to deleting it.")
    email_address: Optional[StrictStr] = Field(default=None, description="Destination address for `type: email`. Any deliverable address — a team alias or a ticketing inbox, not necessarily a platform user. Validated and normalized; also the channel's natural identity, so reusing an address updates that channel. Forbidden on every other type.")
    url: Optional[StrictStr] = Field(default=None, description="Destination endpoint for `type: webhook`. Must be `https` and must resolve to a public address — private, loopback, link-local, and cloud-metadata targets are rejected with a `422` naming the reason. Also the channel's natural identity. Forbidden on every other type.")
    label: Optional[StrictStr] = Field(default=None, description="Human-readable name, shown wherever the channel is listed. Required for `slack` and `teams` (for `teams` it is the only non-secret identifier you get back). Forbidden on the other types.")
    slack_channel_id: Optional[StrictStr] = Field(default=None, description="Slack channel id (not its name) for `type: slack`; the organization's Slack app must already be installed. Also the channel's natural identity. Forbidden on every other type.")
    teams_workflow_url: Optional[StrictStr] = Field(default=None, description="Microsoft Teams Workflows incoming-webhook URL for `type: teams`. Write-only: it is stored as a secret and never returned by any endpoint — reads expose only `has_workflow_url`. Required on create, optional on edit-by-id (omit to keep the stored URL). Must be `https`, resolve to a public address, and sit on the Power Automate host allowlist. Forbidden on every other type.")
    subscriptions: Optional[List[ChannelSubscriptionInput]] = Field(default=None, description="The complete set of alert types to deliver to this channel. **Replaces** the channel's existing set — an omitted or empty list unsubscribes it from everything. At most one entry per alert type; duplicates are a `422`.")
    __properties: ClassVar[List[str]] = ["id", "type", "enabled", "email_address", "url", "label", "slack_channel_id", "teams_workflow_url", "subscriptions"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ChannelUpsertRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in subscriptions (list)
        _items = []
        if self.subscriptions:
            for _item_subscriptions in self.subscriptions:
                _items.append(_item_subscriptions.to_dict() if _item_subscriptions is not None else None)
            _dict['subscriptions'] = _items
        # set to None if id (nullable) is None
        # and model_fields_set contains the field
        if self.id is None and "id" in self.model_fields_set:
            _dict['id'] = None

        # set to None if email_address (nullable) is None
        # and model_fields_set contains the field
        if self.email_address is None and "email_address" in self.model_fields_set:
            _dict['email_address'] = None

        # set to None if url (nullable) is None
        # and model_fields_set contains the field
        if self.url is None and "url" in self.model_fields_set:
            _dict['url'] = None

        # set to None if label (nullable) is None
        # and model_fields_set contains the field
        if self.label is None and "label" in self.model_fields_set:
            _dict['label'] = None

        # set to None if slack_channel_id (nullable) is None
        # and model_fields_set contains the field
        if self.slack_channel_id is None and "slack_channel_id" in self.model_fields_set:
            _dict['slack_channel_id'] = None

        # set to None if teams_workflow_url (nullable) is None
        # and model_fields_set contains the field
        if self.teams_workflow_url is None and "teams_workflow_url" in self.model_fields_set:
            _dict['teams_workflow_url'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ChannelUpsertRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "type": obj.get("type"),
            "enabled": obj.get("enabled") if obj.get("enabled") is not None else True,
            "email_address": obj.get("email_address"),
            "url": obj.get("url"),
            "label": obj.get("label"),
            "slack_channel_id": obj.get("slack_channel_id"),
            "teams_workflow_url": obj.get("teams_workflow_url"),
            "subscriptions": [ChannelSubscriptionInput.from_dict(_item) for _item in obj["subscriptions"]] if obj.get("subscriptions") is not None else None
        })
        return _obj


