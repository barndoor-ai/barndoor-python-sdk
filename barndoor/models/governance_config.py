# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List, Optional
from barndoor.models.default_model_access import DefaultModelAccess
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class GovernanceConfig(BaseModel):
    """
    GovernanceConfig
    """ # noqa: E501
    default_model_access: Optional[DefaultModelAccess] = None
    require_pricing_for_mappings: StrictBool
    require_routing_policy: Optional[StrictBool] = Field(default=None, description="Whether callers must address a routing policy rather than naming a model directly (BCP-4037).  `false` — the historical behaviour — is the `Default`, and must agree with the column default in `V78__require_routing_policy.sql` for the same reason [`DefaultModelAccess`] must agree with V70's: `governance_config` holds a row only for orgs that have changed some governance setting, so whether an org gets this default or the column's depends on whether a row happens to exist. `default_matches_column_default` pins both.  `#[serde(default)]` so config bodies written before this field existed — every current Terraform config and API client — keep deserialising, and keep meaning \"off\".")
    __properties: ClassVar[List[str]] = ["default_model_access", "require_pricing_for_mappings", "require_routing_policy"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GovernanceConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GovernanceConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "default_model_access": obj.get("default_model_access"),
            "require_pricing_for_mappings": obj.get("require_pricing_for_mappings"),
            "require_routing_policy": obj.get("require_routing_policy")
        })
        return _obj


