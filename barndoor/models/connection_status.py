# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import List, Optional
from typing import Union, Any, List, Set, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal, Self
from pydantic import Field

CONNECTIONSTATUS_ANY_OF_SCHEMAS = ["List[Optional[str]]", "str"]

class ConnectionStatus(BaseModel):
    """
    Filter by connection status: connected, available, pending, error, not_connected. For multiple values, repeat the parameter (e.g., ?connection_status=connected&connection_status=available). 'not_connected' expands to ['pending', 'error', 'available'].
    """

    # data type: List[Optional[str]]
    anyof_schema_1_validator: Optional[List[Optional[StrictStr]]] = None
    # data type: str
    anyof_schema_2_validator: Optional[StrictStr] = Field(default=None, description="Order by field: name, created_at, or attention (default: name). attention (BCP-3664) sorts attention-needing servers first — Pending Publish, Connection Error, Pending Credentials, Pending Connection, then Available — tie-broken by name ASC, then id ASC (the id term is what makes the order total, so paging is deterministic); `order` is ignored for this field. The rank is computed in SQL from DB-derived state, so it can disagree with the Bao-recomputed `can_manage_policies` the legacy (non-DB-first) listing path returns. Unrecognized values fall back to created_at rather than erroring — this parameter is under the public API stability contract, so the advertised enum narrows generated clients without tightening server-side validation.")
    if TYPE_CHECKING:
        actual_instance: Optional[Union[List[Optional[str]], str]] = None
    else:
        actual_instance: Any = None
    any_of_schemas: Set[str] = { "List[Optional[str]]", "str" }

    model_config = {
        "validate_assignment": True,
        "protected_namespaces": (),
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_anyof(cls, v):
        if v is None:
            return v

        instance = ConnectionStatus.model_construct()
        error_messages = []
        # validate data type: List[Optional[str]]
        try:
            instance.anyof_schema_1_validator = v
            return v
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # validate data type: str
        try:
            instance.anyof_schema_2_validator = v
            return v
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if error_messages:
            # no match
            raise ValueError("No match found when setting the actual_instance in ConnectionStatus with anyOf schemas: List[Optional[str]], str. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        if json_str is None:
            return instance

        error_messages = []
        # deserialize data into List[Optional[str]]
        try:
            # validation
            instance.anyof_schema_1_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.anyof_schema_1_validator
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into str
        try:
            # validation
            instance.anyof_schema_2_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.anyof_schema_2_validator
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if error_messages:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ConnectionStatus with anyOf schemas: List[Optional[str]], str. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], List[Optional[str]], str]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
