# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from barndoor.models.channel_subscription import ChannelSubscription
from barndoor.models.channel_type import ChannelType
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ChannelResponse(BaseModel):
    """
    A channel and its subscriptions. Secrets (webhook signing secret, slack bot token) live in OpenBao and are never surfaced here except as a one-time reveal via ``signing_secret``, populated only on the response to the request that just generated it (webhook create or regenerate); slack channels expose only the non-secret ``label`` + ``slack_channel_id``.
    """ # noqa: E501
    id: UUID = Field(description="Server-assigned channel id. Stable across updates; pass it as `id` to edit by id.")
    type: ChannelType = Field(description="The kind of destination. See `ChannelType`.")
    enabled: StrictBool = Field(description="Whether the channel currently delivers.")
    user_id: Optional[UUID] = Field(description="Owning user for a personal channel (`in_app`, `user_email`). `null` for the org-wide types, which belong to the organization rather than one user.")
    email_address: Optional[StrictStr] = Field(description="Destination address for `type: email`, normalized. `null` for every other type.")
    url: Optional[StrictStr] = Field(description="Destination endpoint for `type: webhook`. `null` for every other type.")
    label: Optional[StrictStr] = Field(description="Human-readable name, set for `slack` and `teams`. `null` for the other types.")
    slack_channel_id: Optional[StrictStr] = Field(description="Slack channel id for `type: slack`. `null` for every other type.")
    subscriptions: List[ChannelSubscription] = Field(description="The alert types this channel currently delivers. Empty means it delivers nothing.")
    created_at: datetime = Field(description="When the channel was created (UTC).")
    updated_at: datetime = Field(description="When the channel or its subscriptions last changed (UTC).")
    has_signing_secret: Optional[StrictBool] = Field(default=False, description="Whether a `webhook` channel has a stored signing secret — always true for a webhook channel, false otherwise. The secret itself is never readable back; use `POST /channels/{channel_id}/regenerate-secret` to obtain a fresh one.")
    has_workflow_url: Optional[StrictBool] = Field(default=False, description="Whether a `teams` channel has a stored Workflows URL. The URL is a secret and is never returned; this flag is the only readable signal that one is configured.")
    signing_secret: Optional[StrictStr] = Field(default=None, description="One-time reveal of a newly generated webhook signing secret. Present **only** on the response to the request that created it — the `PUT` that first creates a `webhook` channel — and `null` on every list or subsequent read. Store it when you receive it; the only way to recover from losing it is to rotate.")
    __properties: ClassVar[List[str]] = ["id", "type", "enabled", "user_id", "email_address", "url", "label", "slack_channel_id", "subscriptions", "created_at", "updated_at", "has_signing_secret", "has_workflow_url", "signing_secret"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ChannelResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in subscriptions (list)
        _items = []
        if self.subscriptions:
            for _item_subscriptions in self.subscriptions:
                _items.append(_item_subscriptions.to_dict() if _item_subscriptions is not None else None)
            _dict['subscriptions'] = _items
        # set to None if user_id (nullable) is None
        # and model_fields_set contains the field
        if self.user_id is None and "user_id" in self.model_fields_set:
            _dict['user_id'] = None

        # set to None if email_address (nullable) is None
        # and model_fields_set contains the field
        if self.email_address is None and "email_address" in self.model_fields_set:
            _dict['email_address'] = None

        # set to None if url (nullable) is None
        # and model_fields_set contains the field
        if self.url is None and "url" in self.model_fields_set:
            _dict['url'] = None

        # set to None if label (nullable) is None
        # and model_fields_set contains the field
        if self.label is None and "label" in self.model_fields_set:
            _dict['label'] = None

        # set to None if slack_channel_id (nullable) is None
        # and model_fields_set contains the field
        if self.slack_channel_id is None and "slack_channel_id" in self.model_fields_set:
            _dict['slack_channel_id'] = None

        # set to None if signing_secret (nullable) is None
        # and model_fields_set contains the field
        if self.signing_secret is None and "signing_secret" in self.model_fields_set:
            _dict['signing_secret'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ChannelResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "type": obj.get("type"),
            "enabled": obj.get("enabled"),
            "user_id": obj.get("user_id"),
            "email_address": obj.get("email_address"),
            "url": obj.get("url"),
            "label": obj.get("label"),
            "slack_channel_id": obj.get("slack_channel_id"),
            "subscriptions": [ChannelSubscription.from_dict(_item) for _item in obj["subscriptions"]] if obj.get("subscriptions") is not None else None,
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at"),
            "has_signing_secret": obj.get("has_signing_secret") if obj.get("has_signing_secret") is not None else False,
            "has_workflow_url": obj.get("has_workflow_url") if obj.get("has_workflow_url") is not None else False,
            "signing_secret": obj.get("signing_secret")
        })
        return _obj


