# coding: utf-8

"""
    Barndoor.ai Public API

    Unified public API specification for Barndoor.ai platform

    The version of the OpenAPI document: 2.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from barndoor.models.scope_type import ScopeType
from barndoor.models.traffic_type import TrafficType
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class WithAttributionRateLimitPolicy(BaseModel):
    """
    An admin-API response for an entity whose core type lives in `ai_governance` and so cannot carry portal-only columns.  `TokenBudget` and `RateLimitPolicy` are read by the enforcement path on every request; hanging five display-only fields off them would put portal concerns in the hot path's type and force every governance consumer to carry them. Wrapping instead keeps the enforcement reads (`get_enabled_by_org` and friends) returning the bare governance type, while the admin reads return this.  Both halves are `#[serde(flatten)]`, so the wire shape stays the flat object the portal already consumes — same choice `AdminModelPricing` makes.
    """ # noqa: E501
    created_by_email: Optional[StrictStr] = None
    created_by_name: Optional[StrictStr] = None
    created_by_user_id: Optional[UUID] = None
    last_change_note: Optional[StrictStr] = Field(default=None, description="Optional free text explaining *why* the last change was made (\"Raised for the Q3 batch job\", \"Vendor deprecated the old endpoint\").  Admin-authored and rendered in the portal, so it is untrusted input: length-capped here, escaped on render.")
    last_modified_at: Optional[datetime] = Field(default=None, description="When the change named by `last_modified_by_*` happened.  Its own column rather than the row's `updated_at`, because `updated_at` is bumped by writes nobody is attributed for -- pairing the two would date a named admin's edit to a background job's clock. See the V75 migration for the full reasoning.")
    last_modified_by_email: Optional[StrictStr] = None
    last_modified_by_name: Optional[StrictStr] = None
    last_modified_by_user_id: Optional[UUID] = None
    enabled: StrictBool
    id: UUID
    member_of_group: Optional[StrictStr] = Field(default=None, description="BCP-3814 membership filter: when set, this policy only applies to requesters whose IdP group membership includes this group name.  Only meaningful on a broad user-scoped policy (`scope_type == User` with `scope_id` and `scope_value` both `None`), where the counter key resolves to the requesting user's id — so one policy row gives every *member of the group* their own RPM/TPM allowance, and a user joining the group is limited on their next request with no admin action. DB-enforced by `rate_limit_policies_member_of_group_requires_broad_user` (V66, tightened to exclude `scope_value` by V67).  `None` ⇒ org-wide (the pre-BCP-3814 behaviour). Contrast with a `ScopeType::Group` policy, which keys its counter by the group name and so *pools* one limit across all members.")
    name: StrictStr
    org_id: UUID
    requests_per_minute: Optional[StrictInt] = None
    scope_id: Optional[UUID] = None
    scope_type: ScopeType
    scope_value: Optional[StrictStr] = None
    target_mcp_server_id: Optional[UUID] = Field(default=None, description="BCP-4056 Target dimension: when set, the policy only fires on requests routed to this MCP server. The MCP-traffic counterpart of `target_provider_id`, and mutually exclusive with all three LLM target columns (DB-enforced by `rate_limit_policies_mcp_target_exclusive_check`) — no single request carries both an MCP server and an LLM route, so a row naming both could never match.  Distinct from a `ScopeType::McpServer` policy, which puts the server on the *identity* axis and so cannot be combined with a user or group. Unlike the other two object-shaped scopes, that one does work — it moves here for consistency and to make \"this user's rpm on this server\" expressible, not to fix a defect.")
    target_model_alias: Optional[StrictStr] = Field(default=None, description="BCP-4056 Target dimension: when set, the policy only fires when the requested model alias (`request.model`) equals this value. Mutually exclusive with the other two LLM `target_*` fields.  Together with `target_provider_id`/`target_upstream_model` this replaces `ScopeType::Model`, which could never match anything: `GovernanceIdentity::scope_keys()` deliberately emits no `(Model, _)` pair, so a model-scoped policy rendered as Enabled while enforcing nothing.")
    target_provider_id: Optional[UUID] = Field(default=None, description="BCP-4056 Target dimension: when set, the policy only fires on routes whose provider id equals this value. May be combined with `target_upstream_model` to limit a single model on this provider; mutually exclusive with `target_model_alias` (DB-enforced by `rate_limit_policies_alias_target_exclusive_check`).  This replaces `ScopeType::LlmProvider` as the way to express \"limit this provider\". That scope never worked on the check path — the gateway middleware builds its identity with no `llm_provider_id`, so no `(LlmProvider, _)` scope key exists to match against, while the post-response record path *does* carry one and wrote usage to a counter nothing read.")
    target_upstream_model: Optional[StrictStr] = Field(default=None, description="BCP-4056 Target dimension refining `target_provider_id`: when set, the policy only fires on routes whose resolved upstream model name equals this value (string match). Always set alongside a provider (V74 CHECK-enforced) — a bare upstream name would string every provider serving that model into one limit.")
    tokens_per_minute: Optional[StrictInt] = None
    traffic_type: TrafficType
    __properties: ClassVar[List[str]] = ["created_by_email", "created_by_name", "created_by_user_id", "last_change_note", "last_modified_at", "last_modified_by_email", "last_modified_by_name", "last_modified_by_user_id", "enabled", "id", "member_of_group", "name", "org_id", "requests_per_minute", "scope_id", "scope_type", "scope_value", "target_mcp_server_id", "target_model_alias", "target_provider_id", "target_upstream_model", "tokens_per_minute", "traffic_type"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WithAttributionRateLimitPolicy from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if member_of_group (nullable) is None
        # and model_fields_set contains the field
        if self.member_of_group is None and "member_of_group" in self.model_fields_set:
            _dict['member_of_group'] = None

        # set to None if requests_per_minute (nullable) is None
        # and model_fields_set contains the field
        if self.requests_per_minute is None and "requests_per_minute" in self.model_fields_set:
            _dict['requests_per_minute'] = None

        # set to None if scope_id (nullable) is None
        # and model_fields_set contains the field
        if self.scope_id is None and "scope_id" in self.model_fields_set:
            _dict['scope_id'] = None

        # set to None if scope_value (nullable) is None
        # and model_fields_set contains the field
        if self.scope_value is None and "scope_value" in self.model_fields_set:
            _dict['scope_value'] = None

        # set to None if target_mcp_server_id (nullable) is None
        # and model_fields_set contains the field
        if self.target_mcp_server_id is None and "target_mcp_server_id" in self.model_fields_set:
            _dict['target_mcp_server_id'] = None

        # set to None if target_model_alias (nullable) is None
        # and model_fields_set contains the field
        if self.target_model_alias is None and "target_model_alias" in self.model_fields_set:
            _dict['target_model_alias'] = None

        # set to None if target_provider_id (nullable) is None
        # and model_fields_set contains the field
        if self.target_provider_id is None and "target_provider_id" in self.model_fields_set:
            _dict['target_provider_id'] = None

        # set to None if target_upstream_model (nullable) is None
        # and model_fields_set contains the field
        if self.target_upstream_model is None and "target_upstream_model" in self.model_fields_set:
            _dict['target_upstream_model'] = None

        # set to None if tokens_per_minute (nullable) is None
        # and model_fields_set contains the field
        if self.tokens_per_minute is None and "tokens_per_minute" in self.model_fields_set:
            _dict['tokens_per_minute'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WithAttributionRateLimitPolicy from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "created_by_email": obj.get("created_by_email"),
            "created_by_name": obj.get("created_by_name"),
            "created_by_user_id": obj.get("created_by_user_id"),
            "last_change_note": obj.get("last_change_note"),
            "last_modified_at": obj.get("last_modified_at"),
            "last_modified_by_email": obj.get("last_modified_by_email"),
            "last_modified_by_name": obj.get("last_modified_by_name"),
            "last_modified_by_user_id": obj.get("last_modified_by_user_id"),
            "enabled": obj.get("enabled"),
            "id": obj.get("id"),
            "member_of_group": obj.get("member_of_group"),
            "name": obj.get("name"),
            "org_id": obj.get("org_id"),
            "requests_per_minute": obj.get("requests_per_minute"),
            "scope_id": obj.get("scope_id"),
            "scope_type": obj.get("scope_type"),
            "scope_value": obj.get("scope_value"),
            "target_mcp_server_id": obj.get("target_mcp_server_id"),
            "target_model_alias": obj.get("target_model_alias"),
            "target_provider_id": obj.get("target_provider_id"),
            "target_upstream_model": obj.get("target_upstream_model"),
            "tokens_per_minute": obj.get("tokens_per_minute"),
            "traffic_type": obj.get("traffic_type")
        })
        return _obj
